# ai-scan-job-skills
Chrome extension to scan job posts and get the required skills for that certain job

# run backend
cd backend
uvicorn src.main:app --reload

# run frontend
cd frontend
npm run dev

#build to import to chrome extension
npm run build